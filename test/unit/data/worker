self.importScripts(
  '/node_modules/babel-polyfill/dist/polyfill.js',
  '/dist/planck-core.js'
);

self.onmessage = function(message) {
  self.postMessage({
    isBrowser: Planck.Environment.isBrowser,
    isWorker: Planck.Environment.isWorker,
    isNode: Planck.Environment.isNode,
  });
};
